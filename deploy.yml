---
- hosts: Appservers
  become: true
  tasks: 
  - name: copy the war file
    copy: 
      src: target/hello.war
      dest: /opt/tomcat/webapps/
  - name: stop the tomcat
    shell: nohup /tomcat/bin/shutdown.shell
  - name: starting the tomcat
    shell: nohup /tomcat/bin/startup.sh  
