---
- hosts: Appservers
  become: true
  tasks: 
  - name: copy the war file
    copy: 
      src: target/hari
      dest: /tomcat/webapps/
      remote_src: true
  - name: stop the tomcat
    shell: nohup /opt/tomcat/bin/shutdown.shell
  - name: starting the tomcat
    shell: nohup /opt/tomcat/bin/startup.sh  
